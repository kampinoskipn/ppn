document.addEventListener("DOMContentLoaded",()=>{if("/komunikaty-turystyczne"===window.location.pathname){var imgs=document.querySelectorAll(".information__content img");imgs.length>0&&(imgs.forEach((function(img){let url=img.src,cl,alt;var template=`<a href=${url} class="glightbox">\n\t\t\t<img class=${img.className} src=${url} alt="${img.alt}" height="84">\n\t\t\t</a>`;img.parentElement.insertAdjacentHTML("beforeend",template),img.remove()})),GLightbox())}});